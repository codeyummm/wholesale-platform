const axios = require('axios');
require('dotenv').config({ path: './.env' });
async function run() {
  const token = "mock"; // I don't have an ebay token, I can't call eBay API easily
  console.log("We need to see what properties are in conversationsMap");
  process.exit(0);
}
run();
